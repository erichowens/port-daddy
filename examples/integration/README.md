# Integration Signals Example

## ready-needs.sh

Demonstrates cross-agent coordination using integration signals: one agent declares readiness, another declares a need.

```bash
bash examples/integration/ready-needs.sh
```

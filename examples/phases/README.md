# Session Phases Example

## session-lifecycle.sh

Demonstrates full session lifecycle: create session, advance through phases, claim files, add notes at each phase.

```bash
bash examples/phases/session-lifecycle.sh
```
